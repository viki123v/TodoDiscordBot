services:
  db:
    container_name: todo_bot_storage
    image: postgres:latest
    env_file:
      - ./.env
    ports:
      - "6000:5432"
    volumes:
      - ./DDL/init.sql:/docker-entrypoint-initdb.d/init.sql
    command: postgres & sleep 2

